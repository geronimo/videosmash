== Welcome to VIDEOSMASH

=== Setup

1. At the command prompt:
       bundle install
       bundle exec rake db:seed
       bundle exec rails s

2. Go to http://localhost:3000/ and you'll see the VIDEOSMASH logo and a Sign up button

3. Sign Up and start using the app

=== Tests

1. At the command prompt:
       bundle exec cucumber
